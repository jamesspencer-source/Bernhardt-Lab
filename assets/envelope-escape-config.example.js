// Optional config for a shared/global leaderboard.
// Copy this file to `assets/envelope-escape-config.js` and set the deployed endpoint.
// Then include it in index.html before `assets/envelope-escape.js`.

window.ENVELOPE_LEADERBOARD_URL = "https://your-worker-domain.example/leaderboard";
